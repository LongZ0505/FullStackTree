server:
  port: 8081
  servlet:
    context-path: /post
spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/post-Tree?authSource=admin
app:
  services:
    identity: http://localhost:8080/identity
    follow: http://localhost:8082/follow